# ".NET Creator" on electron JS

---
## How to install:
- clone project
- Go to terminal/cmd
- Install electron npm packages:
  ```bash
  npm install electron --save -dev
  ```

- Start project:
  ```bash
  npm start
  ```
